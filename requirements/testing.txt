# Kept in step with dev/Containerfile and with setup.py's `tests_require`.
#
# Upgrade plan Stage 9 is the second stage that moves this file; Stage 7 was
# the first, and the Stage 7 section of `docs/upgrade-plan.rst` records that
# move. 3.10.0 covers Django 1.8 to 3.0, declares `pytest>=3.6`, and is the
# last pytest-django that runs on Python 2.7, so the pin holds from here
# through Stage 12 -- Appendix A of the plan gives the ladder.
#
# 3.10.0 reads a test's marks through `node.iter_markers()`, where 3.1.2 read
# them through `MarkInfo`. That one plugin line was 456 warnings and the only
# `filterwarnings` entry `pytest.ini` carried since Stage 7; the move to
# 3.10.0 deleted the entry.
pytest-django==3.10.0

# 4.6.11 is the last pytest in the 4.x series and the last release that runs
# on Python 2.7 (`requires_python` is `>=2.7, !=3.0.*, !=3.1.*, !=3.2.*,
# !=3.3.*`; pytest 5 requires Python 3), so this pin also holds through
# Stage 12. pytest-cov is still not installed: `coverage run -m pytest` is
# what `dev/kasvimuseo app coverage` and the CI step both run, and one
# command is easier to keep honest than two.
pytest==4.6.11

# 5.5 is the last coverage that runs on Python 2.7 (`requires_python` is
# `>=2.7, !=3.0.*, !=3.1.*, !=3.2.*, !=3.3.*, !=3.4.*, <4`), so this pin is the
# end of the ladder until the Python 3 flip at Stage 10. Appendix A of
# `docs/upgrade-plan.rst` schedules it here, and Part 2.6 calls the 4.5.4 pin
# before it "conservative".
#
# One consequence for the gate rather than for the measurement: 5.5 adds the
# `precision` setting that 4.5.4 has no equivalent of, and `precision` is what
# decides whether `fail_under` rounds. Measured in this image on a module at an
# exact 96.77 %: with no `precision`, `fail_under = 97` exits 0, the same
# rounding 4.5.4 did; with `precision = 2` it exits 2. `.coveragerc` sets it and
# argues why.
coverage==5.5

mock==2.0.0
pbr==4.0.2
