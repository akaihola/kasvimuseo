# Kept in step with dev/Containerfile. pytest-django 2.9.1 is the last release
# supporting Django 1.5, and it needs pytest < 3.6; that in turn rules out
# pytest-cov, so coverage is measured with `coverage run -m pytest`.
pytest==3.5.0
pytest-django==2.9.1
coverage==4.5.4
mock==2.0.0
pbr==4.0.2
