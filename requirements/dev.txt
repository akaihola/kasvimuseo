# The development set: everything the application needs to run, plus the tools
# that are only ever useful on a developer's machine. `dev/Containerfile`
# installs this file, so the development image has them and the production
# image -- which installs `production.txt` -- does not.
-r production.txt

# `runserver_plus` and `shell_plus`, with the Werkzeug debugger the first of
# them needs. Development only since upgrade plan Stage 0: it is in nobody's
# import list, and `ylaneenkasvit/local_settings.development.py` is what puts
# it into INSTALLED_APPS. `runserver_plus` is also what replaced the removed
# `django-pserver` (issue 033) -- reach it with
# `dev/kasvimuseo app manage runserver_plus`; `app run` serves with gunicorn
# (issue 044). 2.2.9 is the last release that supports both Python 2.7 and
# Django 1.11 (upgrade plan Stage 9), so it is the pin until the Python 3
# flip.
django-extensions==2.2.9
Werkzeug==0.8.3

# django-extensions' own dependency -- `six>=1.2` in its metadata, imported by
# 22 of its modules -- and nobody else's here, which is why it moved out of
# `production.txt` with it. Pinned rather than resolved because
# `dev/Containerfile` installs this file with `--no-deps`.
six==1.11.0

# django-extensions' as well, and only below Python 3.5: 2.2.9 declares
# `typing ; python_version < "3.5"`, and its admin module imports it, so the
# `--no-deps` install needs the pin. 3.10.0.0 is the last release, and its
# own metadata stops at Python 3.5 -- delete this backport at the Python 3
# flip (upgrade plan Stage 10) with the interpreter that needs it.
typing==3.10.0.0
