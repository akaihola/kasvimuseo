# The browser tests run on the host's Python 3, not in the Python 2.7 app
# container, so nothing here is installed anywhere near the application. uv
# resolves this file per run; see `dev/kasvimuseo app browser-test`.
#
# playwright is pinned loosely on purpose: the browser builds it downloads have
# to match the library, so they must be free to move together. The browsers
# themselves are never installed by the script -- CI runs `playwright install
# <engine>` once per matrix leg, and a development machine points
# PLAYWRIGHT_BROWSERS_PATH at the copies it already has. Two engines since
# issue 061: chromium and webkit.
playwright~=1.61.0
pytest~=8.4
