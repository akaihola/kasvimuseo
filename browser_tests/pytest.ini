# The browser tests have an ini of their own so pytest does not pick up the
# repository's, which configures pytest-django for the Python 2.7 suite -- a
# plugin that is not installed here and settings these tests never import.
[pytest]
testpaths = .
python_files = test_*.py
